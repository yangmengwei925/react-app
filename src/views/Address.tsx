import React, { useEffect } from 'react';

let Address: React.FC = (props) => {
  return (
    <div>
    
    </div>
  )
}

const mapStateToProps = (state: any) => {
  return {
    
  }
}

const mapDispatchToProps = (dispatch: Function) => {
  return {
  
  }
}


export default(Address)
